import {} from '../actions/actionTypes';

const initialState = {
	
}

const reducer = (state = initialState, action) => {
	switch(action.Type){
	

		default: 
		return state;
	}
}

export default reducer;